import { MultipplicaPorPipePipe } from './multipplica-por.pipe.pipe';

describe('MultipplicaPorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MultipplicaPorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
